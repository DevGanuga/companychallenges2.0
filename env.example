# =============================================================================
# Company Challenges - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
#
# For production deployment (Vercel, etc.):
# - Add all these variables in your deployment platform's environment settings
# - Make sure all NEXT_PUBLIC_* variables are available at build time
# - The SUPABASE_SERVICE_ROLE_KEY must be set for admin operations

# -----------------------------------------------------------------------------
# Demo Mode (Optional)
# -----------------------------------------------------------------------------
# Set to 'true' to bypass authentication (useful for demos/previews)
# NEXT_PUBLIC_DEMO_MODE=true

# -----------------------------------------------------------------------------
# Clerk Authentication
# -----------------------------------------------------------------------------
# Get these from your Clerk dashboard: https://dashboard.clerk.com
# Required if NEXT_PUBLIC_DEMO_MODE is not 'true'
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx

# Clerk redirect URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/admin
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/admin

# -----------------------------------------------------------------------------
# Supabase (REQUIRED)
# -----------------------------------------------------------------------------
# Get these from your Supabase project: Settings > API
#
# Project URL - looks like: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon Key - "anon public" key (safe to expose in client code)
# Used for public data access with RLS policies
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role Key - "service_role" key (KEEP SECRET!)
# ⚠️ WARNING: This key bypasses ALL Row Level Security policies
# ⚠️ Never expose in client code or commit to version control
# ⚠️ Required for: admin panel, server actions, API routes
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# -----------------------------------------------------------------------------
# App Configuration
# -----------------------------------------------------------------------------
# Base URL for the app (used for generating absolute URLs)
# Local: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Analytics (Optional)
# -----------------------------------------------------------------------------
# Google Analytics 4 Measurement ID
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# TROUBLESHOOTING
# =============================================================================
# If you see "Invalid API key" error:
# 1. Check that SUPABASE_SERVICE_ROLE_KEY is correctly set
# 2. Make sure you're using the service_role key (not the anon key) for admin
# 3. Verify the key is from the correct Supabase project
# 4. Ensure environment variables are loaded (restart dev server after changes)
#
# If you see "Missing environment variables":
# 1. Make sure .env.local exists with all required values
# 2. For production, ensure all vars are set in deployment platform
# 3. Variables starting with NEXT_PUBLIC_ must be available at build time